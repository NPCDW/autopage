{
  "image": "selenium/standalone-chrome:latest",
  "postCreateCommand": "sudo apt update -y && sudo curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py && sudo python3 get-pip.py && pip3 install --user -r requirements.txt"
}