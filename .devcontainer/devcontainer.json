{
  "image": "selenium/standalone-chrome:latest",
  "postCreateCommand": "sudo apt update -y && sudo apt install -y git && sudo curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py && sudo python3 get-pip.py && sudo pip3 install -r requirements.txt"
}